#include <stdlib.h>
#include <time.h>
#include <stdio.h>
/**
 * main - This is a description
 * Return: int will be returned
 */
int main(void)
{
char be='a' ;
char bee = '\n';

for (be == 'a'; be <= 'z'; be++)
{
if (be != 'e' && be != 'q')
putchar(be);
}
putchar(bee);
return (0);
}
